# TrackAll Desktop — Environment Variables
# Copy to .env and fill in values

# URL of the TrackAll web frontend (default for dev)
TRACKALL_APP_URL=http://localhost:3000

# Set to 1 when running in development
ELECTRON_IS_DEV=0

# GitHub token for auto-updater (if using private repo)
GH_TOKEN=
